# global

Global configuration and shared resources repository.

## Overview

This repository contains shared configurations, utilities, and resources used across multiple projects.

## Structure

- `/config` - Configuration files
- `/scripts` - Utility scripts
- `/docs` - Documentation

## Usage

This repository can be used as a git submodule or referenced in other projects.

## Deployment

Automatic deployment is configured via GitHub Actions.
